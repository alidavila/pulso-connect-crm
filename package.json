{
  "name": "pulso-connect-crm",
  "version": "1.0.0",
  "description": "PulsoConnect Command Center — Dashboard unificado de todos los sistemas",
  "scripts": {
    "build": "echo 'Static site — no build needed'"
  }
}
